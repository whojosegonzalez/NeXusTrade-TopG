{
  "name": "@nexustrade/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared NeXusTrade types and helpers.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.57"
  }
}
